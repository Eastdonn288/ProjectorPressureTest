# PPTP — 待办事项

> 本文件只放**已确认、但当前决定不做**的事情。已完成的进 [CHANGELOG.md](CHANGELOG.md),
> 设计决策进 [docs/DECISIONS.md](docs/DECISIONS.md),踩坑与规矩进 [docs/PITFALLS.md](docs/PITFALLS.md)。
> 用户 2026-09-11 明确:下面这些**先记录,暂不修改**。
>
> **编号是标识符,不复用也不重排。**条目被移除后留下的号是空的,不拿新内容去填 ——
> 否则任何外部转引("见 TODO §7.1")都会指向别的东西。

---

## 总览(2026-09-21 梳理)

> 这一节**故意不占编号** —— 编号空间是待办项专用的(见上面那条纪律)。

正文按**引入时间**排(§1…§10),所以**读的顺序和该做的顺序是反的**。这张表补上优先级 --
**具体内容一律不看这里,回正文对应编号**。

### 优先级怎么分

| 档 | 含义 |
|---|---|
| **P0** | 会产出一份**看起来正常的错结论**(假绿)。这类永远排第一,不看成本。 |
| **P1** | 该做,且**条件已经具备** -- 影响可信度或用户可见行为。 |
| **P2** | 该做,但**要先有特定条件**(真机场景 / 长跑基线 / 负载环境)才能动。 |
| **P3** | 已经裁决**不做**。留着只是为了知道形状,**不要顺手改**。 |

**成本** = 从动手到"验证完"的量级:**小** ≈ 一小时内;**中** ≈ 半天量级;**大** ≈ 要重设计或依赖外部。

### P0 — 假绿(会产出一份看起来正常的错结论)

| 编号 | 一句话 | 影响范围 | 成本 |
|---|---|---|---|
| §1 | `wifi_reboot_stress.py` 把"失败的 reboot"记成 PASS,最后还打印 `success rate: 100.0%` | 3 个 reboot 脚本(wifi / bt / sensor 同一结构,**很可能一起中招**) | 中 -- 判定链路补一步"确认设备确实掉线过",rc 与"从未观察到掉线"要进报告 JSON |
| §6.10 | gate 8 分不清「运行太短」与「代码把通道关掉了」,后者会被判 pass | 判定模型 + evidence schema + 判定版本号 | 中 -- 要加一个**与"到期"无关**的量(理论开启次数),bug 改不动它才算独立 |

### P1 — 该做,条件已具备

| 编号 | 一句话 | 影响范围 | 成本 |
|---|---|---|---|
| §7.2 | `wifi_onoff` / `sensor_reboot` / `battery_inout` 的报告**没在真机上跑过** | 3 个脚本的报告产出 | 小 -- 连上设备各跑一轮 |
| §4 + §6.8 | 前端**从未在真实浏览器里被验证过**(按钮 / 存档 / 回传 / 折线滚动全是"没测过") | 整个前端 | 小(人工)-- 开浏览器 Ctrl+F5,清单在正文 |
| §6.2 | 判定阈值(`delta` / `slope` / 最短观测窗)是**设计假设而非实测值** | 整个判定模型 -- 决定"漏报 / 误报" | 中 -- 要一次真机**健康长跑**基线,只有它能解 |
| §6.23 | NTC 换 `su 0` 后,README / PERF_MONITOR_V2 里那两个耗时数字**没重量**(现在偏小) | 两处文档数字 | 小 -- 一次 `--probe-cost` + 改数 |
| §6.11 | `capped_by` 算出来了但**没有任何渲染层读它**(伪门从没在报告上露过面) | **每一份**报告的判定块 | 小 -- 一处渲染,但改动面广,要单独眼验 |
| §6.12 | `NetworkCapabilities`(比 ping 更好的 L3 信号)没读到真 token,所以没写解析器 | wifi 通道的判定质量 | 小 -- 先读一次真输出,再谈写不写 |
| §7.8 | `app_launch_stress` 的**热启动(hot)**模式没真机验证(冷启动已过) | 1 个脚本 | 小 -- 跑一轮核对 WARM/HOT 语义 |
| §9.3 | 删除任务的确认框**在说谎**(说日志会一起删),与同界面 `×` 的 tooltip 互相矛盾 | `static/app.js` 两处文案 | 小 -- 改字符串,措辞正文里给了 |

### P2 — 该做,但要先有特定条件

| 编号 | 一句话 | 影响范围 | 成本 |
|---|---|---|---|
| §6.4 | `pidof <pkg>` 不枚举 `com.x:remote` 子进程,`fg_cpu` 覆盖可能**变窄而不是变宽** | fg 通道读数 | 小 -- 但手边没有多进程前台 app(要 Netflix / YouTube 这类) |
| §6.5 | **SoC 温度**(thermal zone)与**功耗**仍然完全没纳管(NTC 只解决了 LCD / LED 节点) | 新增监控通道 | 中 -- 先探本板可读性,再谈加不加 |
| §6.9 | `watch_pkg` 里"本地媒体播放器"的**进程名是否等于包名**未核(`pidof` 取不到就恒为空) | fg_cpu 对这个 app | 小 -- 一条 adb 命令,但要等它被选中跑 |
| §6.3 | **adb over WiFi 完全没测**,相对收益排序可能反转 | 所有耗时数字 | 中 -- 需要环境 |
| §6.14 + §6.15 第 2 条 | WiFi / NTC 在**"设备真在放视频"**那种负载下的耗时没测 | 文档里的耗时数字 | 小 -- 但要开着视频跑 |
| §2 | 中断早于第一轮时,汇总读起来像"设备彻底失败" | 几个 reboot 脚本 | 小 |
| §6.13 | 每段 guard 之和可能超过 PC 侧预算 `B`(失败模式**响亮**,不静默) | 预算几何 | 中 -- 要重推预算模型,不在小改动里做 |
| §6.7.1 | 硬停的 run 其 CSV 要**人工从 `reports/` 捡**(没有报告就没有配对的锚点) | `reports/` | 小 |
| §7.4 | `app_launch_stress` 一次运行多份报告,前端按钮**只开第一份** | 前端按钮 | 小 -- 要不要加多选 UI **未定** |
| §5 | E-SafeNet 透明加密会破坏文件工具 —— 这是**本机环境问题,不是代码问题** | 所有文件读写 | **不可控** -- 取决于能否让 Agent 走白名单进程;在那之前按 PITFALLS #0 绕 |

### P3 — 已裁决"不做",知道形状就行

| 编号 | 一句话 | 为什么不做 |
|---|---|---|
| §3 | 串口停止时最后半行必丢 | 修它要在最取消敏感的路上插等待,代价与收益不成比例 |
| §6.1 | 归档无限增长 → 磁盘满 | 用户明确否决"开跑前查剩余空间" |
| §6.6 | 硬停时没有最终判定 | 已知边界,不是 bug |
| §6.7 | 报告形状跨版本不同构 | 消费方自己容忍缺字段 |
| §6.20 | `--probe-cost` 在负载下没测准 | **先想清楚"要不要在负载下量逐通道",再动手** |
| §6.21 | 硬停不触发 NTC 换算 | 手工补的命令在正文;不做第二个触发点(#51 的形状) |
| §7.3 | 引擎的 `attach()` / `document_of()` 是死 API | 觉得碍眼就删,没有调用方 |
| §7.5 | 明文 Wi-Fi 口令仍在归档 `report.json` 里 | 用户长期指令:**不算敏感,不要再提** |
| §7.7 | `reports/` 里留着本轮验证产物 | 用户已定:暂不删 |
| §8.2 | 新增 `ir_sequences/` ini 要等服务重启 | 收益极小,不加缓存失效 |
| §8.3 | keepalive 会与每拍采样争用 adb | 只会抬高预算,不会误杀 |
| §8.4 | 中断落在长按中间 → 设备可能停在按下态 | 不在 keepalive 补 `finally`(要动三脚本共用的执行器) |
| §8.5 | 两个线程同时 `print` 极低概率吃掉一个采样点(≈1e-5) | 不加进程级 stdout 锁,属过度设计 |
| §8.6 | `key_sent` 口径 | 保守方向,已接受 |
| §9.1 | 服务重启后日志面板卡住 | 有绕法(重开任务 / 刷新),不做自动重连 |
| §9.2 | 删任务不回收 log 文件 | 刻意不做:`archive/` 是耐用副本,`logs/` 只是工作区 |
| §6.24 | 归档里那张**坏图仍是坏的**(v2.14.2 只修了工具) | 归档是**不可变**的,重出等于改历史产物;要用户点名(命令在正文) |
| §10.1 | `power_cycle_stress` 被**硬停**的那一次没有报告(§6.21 同形状) | 加服务端兜底 = 造第二个报告触发点(#51 的形状) |
| §10.2 | 新脚本的嗅探循环**从未在真机上跑过**(无硬件即无证据) | 它**不是**代码问题,是需要一次真机短跑;正文给了建议命令 |
| §10.3 | 开机后 IR 的 `Long` 步可能把设备停在按下态(窗口比 §8.4 大一档) | 要修得动三脚本共用的 `ir_runner.run_step`;规避:只用 `Short` 步 |
| §10.4 | 报告的"按键"列**不保证 YouTube 真的播起来了** | 不在 IR 的反馈面上;报告已写明,别当成功判据引用 |

### ✅ 已结案(正文保留在原位 -- 那是决策史,不搬走)

`§6.16`(v2.13.1 修)· `§6.17`(`run_probe_cost` 改 round-robin)· `§6.18`(→ D-64)·
`§6.19`(→ D-65 / 66 / 67)· `§6.22`(→ D-68,v2.14.1)

---

## 1. `wifi_reboot_stress.py` 会把「失败的 reboot」记成 PASS

**严重度:高** —— 这是压测平台最坏的输出形式:假绿。

**问题**:脚本从不校验 `adb reboot` 是否真的让设备重启了。

- `adb reboot` 的返回码被丢弃(脚本注释写着 `non-zero is normal`,但对 rc 不做任何判断)。
- "等待设备上线"的轮询(`sys.boot_completed`)对一个**从未重启过**的设备**同样立即成立** —— 设备一直在线,所以第一轮就通过。
- 结果:如果 `adb reboot` 因为任何原因开始失败(ADB/USB 抖动、设备瞬时 offline、固件不允许 reboot、adb server 被杀),脚本会**继续报 PASS 到底**,最后打印 `success rate: 100.0%`。

**为什么会发生**:评测链路是 "发 reboot 命令 → 等设备上线 → 查 WiFi",缺了中间那步"**确认设备确实掉线过**"。

**建议方向**(未定稿):
- 记录 `adb reboot` 前后的 `sys.boot_completed` 或开机时间(`/proc/uptime`)变化,确认确实重启过;
- 或轮询时**先等设备消失**(`adb devices` 里 offline/absent),再等它回来 —— 没观察到掉线就判该轮 FAIL;
- 至少把 rc 与"从未观察到掉线"记进报告 JSON,让假绿可被事后发现。

**同类风险**:`bt_reboot_stress.py` / `sensor_reboot_stress.py` 是同一个结构(reboot → 等上线 → 查回连),
**很可能有同样的问题**,改动时一并核对。

---

## 2. `wifi_reboot_stress.py` 中断早于第一轮时,汇总读起来像彻底失败

**严重度:低** —— 可读性,不影响数据正确性。

**问题**:在第一轮迭代完成前按「中断」,stdout 会打印:

```
========== summary ==========
  passed: 0 / 0
  success rate: 0.0%
```

外加 exit code 1。**实际上一轮都没测**,但这个记录读起来和"设备彻底失败"一模一样 ——
而这是用户唯一会在 console 里看到、也是会进归档的通道。

**建议方向**(未定稿):区分"0 轮通过"与"0 轮执行";中断且 `completed == 0` 时打印
`no iterations completed (interrupted before first check)` 且退出码不要用 FAIL 语义。

**同类风险**:其它 reboot 类脚本的中断汇总同样需要核对。

---

## 3. 串口停止时最后半行必丢(已确认,刻意不修)

**严重度:低** —— 一行。

读线程的 tail flush 只在 `stop_evt` 置位后执行,而 `_stop_captures` 在同一个同步块里
`evt.set()` 紧接 `ctask.cancel()`,消费端先收到 CancelledError 并关掉文件,那半行就进了无人读的队列。
修它需要在最取消敏感的路径里插等待,代价与收益不成比例。

**含义**:以裸提示符(无换行)结尾的设备 console,最后一行永远不进文件。
详见 [docs/PITFALLS.md](docs/PITFALLS.md) #30。

---

## 4. 前端从未在真实浏览器里被验证过

**严重度:低** —— 但这是"没测过",不是"没问题"。

v2.7.0 的服务端做了逐路径真机验证,DOM id 也做了交叉校验,但"按钮长什么样、点下去对不对"
没有人在浏览器里看过。

**下次开浏览器先 Ctrl+F5**,重点看:任务卡 `存档` 按钮、任务面板占用数字、跑完那次 `[archive]`
摘要行、图表回传是否真的落进 `archive/*/chart.png`、perf 折线图右缘是否跟随最新样本滚动,
以及放电测试后设备卡是否变成"放电关机"(虚线红边)而非消失。这些收益**完全是视觉的**,
`py_compile` / `node --check` 测不出来。

---

## 5. ⚠ E-SafeNet 透明加密会破坏 Claude Code 的文件工具

**严重度:高(会毁文件)** —— 这不是项目代码问题,是本机环境问题,但必须记录。
机制、不对称性、规则与逃生口的完整说明在 **[docs/PITFALLS.md](docs/PITFALLS.md) #0**;
本节只留**清单与实测方法**。

**已确认带密文壳(2026-09-16 收尾盘点,逐个 `Read` 实测)**:
`server.py`、`scripts/_pptp_report.py`、`scripts/perf_monitor.py`、`scripts/wifi_onoff_stress.py`、
`scripts/battery_inout_stress.py`。

**当前可正常 Read**:`scripts/wifi_switch_stress.py`、`scripts/wifi_reboot_stress.py`、
`scripts/bt_reboot_stress.py`、`scripts/sensor_reboot_stress.py`、`scripts/app_launch_stress.py`、
`scripts/ir_runner.py`、`static/app.js`、`docs/*.md`、`CHANGELOG.md`。

**2026-09-20 复测更新(v2.13.0 期间)**:

- **`scripts/_pptp_report.py` 这一段是明文**,Read/Edit 全程正常 —— 它在上面的清单里被列为带壳,
  说明**清单本身会过期**。`static/index.html`、`static/style.css` 同样正常。
- **`scripts/perf_monitor.py` 与 `server.py` 仍带壳**(本轮全程只用 Python 脚本改)。
- **壳的状态会在一次会话中间翻转**,且与文件是否被损坏**无关**:同一个文件在本次会话前段
  Read 得到明文、后段变成乱码,代码本身没动过。
- **`Read` 对带壳文件会谎报行数**:`scripts/perf_monitor.py` 实际 **4134+** 行,Read 报的只有几百行;
  而且**返回的是密文,不是空**。行数明显偏小本身就是壳的指纹。
- ⇒ **可靠的读路径是 `git diff` / `py_compile` / `--selftest` / 直接跑脚本**,不是 Read。
  看到"这个文件怎么这么短"或 Grep 0 命中,**先怀疑壳,再怀疑代码不存在**。

⚠ **带壳与否不可预测** —— 上面那批"带壳"里有一半是**本轮被 Python 脚本改过却仍是明文**的,
所以**不能用"我改过它/没改过它"推断**,每个文件碰之前**单独实测**。

**2026-09-23 新增指纹**:壳不只骗 `Read`,**`wc -l` 也骗** —— 同一次 `wc -l` 报
`server.py` **326** 行,而同一条命令报 `static/app.js` 2525 行(接近真值 2526)。Python 读到的是
**2672** 行。所以**别拿 `wc -l` 数这些文件的行数**(写文档/统计代码量时会全错),要数就走 Python:

```bash
python -c "import io,sys;print(sum(1 for _ in io.open(sys.argv[1],encoding='utf-8')))" server.py
```

**便宜的实测方法**:`Read` 那个文件 **`limit: 2`** —— 出乱码或行数明显不对就是带壳。
**别用字节扫描**(在磁盘上 grep `E-SafeNet` 字样):CLAUDE.md / TODO.md 自己的正文里就写着这个词,
必然假阳性 —— 而 Python 读它们本来就是明文,壳是**读进程侧**的东西。

**待办**:是否有办法让 Agent 工具走白名单进程(或反过来避免用非白名单进程写这些文件)。
在那之前,`server.py` 与 `scripts/perf_monitor.py` 一律按 PITFALLS #0 的方式改。

---

## 6. perf_monitor v2 的风险与未验证项(2026-09-14,实现期间不阻塞)

设计见 [docs/PERF_MONITOR_V2.md](docs/PERF_MONITOR_V2.md)。以下**已知且已接受**,不要在实现时
顺手"解决",除非用户单独提出。

> **状态(2026-09-14,v2.8.0)**:P2–P5 已落地并在真机跑通(40s/75s 两次,`RESULT: OK`、全门通过、
> 占空比 9.06%)。**未验证项没有减少多少** —— 真机跑通只证明"能采到数",不证明"判定准"。
> 下面 **6.2(阈值未标定)** 仍是最大的那一条,且**只有一次健康的长跑基线能解**。

### 6.1 归档无限增长 → 磁盘满(用户明确否决磁盘检查)

归档永久保留 + 每次 run 新增 `samples.csv` + logcat 默认 1 GB/次,磁盘**会**满。用户 2026-09-14
明确否决"开跑前查剩余空间"。后果需要知道:

- 满了之后 `_archive_task` **设计上就是静默失败**(不抛出,只写进 `summary.json` 的 notes),
  用户只会看到 `logs/` 里越堆越多;
- 更糟的是**跑在中途**:logcat / serial 写不进去会静默丢数据,而**任务照常报完成**。

### 6.2 判定阈值未标定

`delta`、`slope`、最短观测窗这些常量在拿到一次真机健康长跑基线之前,是**设计假设而非实测值**。
标定完成前它们只能靠 `--selftest` 的合成序列保证"不明显错",不能保证"不漏报不误报"。

### 6.3 adb over WiFi 完全没测

全部实测数字是 USB + 设备重载下(load avg ~21/4 核、399-401 任务)测的。WiFi 下传输基线会大幅
抬升,**相对收益排序可能反转**。`--probe` 必须保留现场重标定入口,文档不得把这些绝对值写成承诺。

### 6.4 多进程 app 的 `pidof` 语义缺口

`top | grep` 能看到 `com.x:remote` 这类子进程行,而 `pidof com.x` **不枚举它**。换数据源后
`fg_cpu` 的覆盖可能**变窄**而不是变宽。本机没有多进程前台 app 可测,落地前必须在真机
(Netflix / YouTube 这类有 `:remote`/`:sandbox` 的 app)上对照验证。

### 6.5 没有温度/功耗指标(**温度已部分解决 —— v2.13.0 加了 NTC**)

对"8 h 无人值守别把平台搞不稳",**SoC 温度可能是最相关也最缺失的信号**。本轮未探过
`/sys/class/thermal/thermal_zone*/temp` 在本板是否可读、是否要 root。

**v2.13.0 部分解决**:加了 **NTC 节点温度**(LCD / LED 两个 iio 原始计数),但 ——

- **落的是原始 ADC,不是摄氏度**:所以 `unit=raw_adc`。换算公式已经拿到(2026-09-20),但裁决
  **不落在采集脚本里**,而是独立的 [tools/ntc_convert.py](tools/ntc_convert.py)(决策见
  [docs/DECISIONS.md](docs/DECISIONS.md) D-63)。**归档的数据不用重跑就能变成摄氏度** ——
  对归档的 `samples.csv` 直接跑那个工具即可;只有历史**报告里的判定行**仍是计数。
  该工具 1.1.0 起同时出一张**温度曲线图**(阶梯 + 真实读数打点),见 D-63 的同日追加。
- **不是 SoC 温度**:thermal zone(`mtk-thermal 90000` / `gpu-thermal 83000`)是另一回事,
  单位毫摄氏度,**仍未纳管**。
- **功耗**仍然完全没有。

### 6.6 硬停时没有最终判定

平台 force-stop 时脚本来不及收尾,归档里只有 ≤120 s 前那份 `partial=true` 的临时判读。
这是已知边界,**不是 bug**。

### 6.7 报告形状跨版本不同构

旧 `report.json` 没有 `verdict` 与新字段。任何报告消费方都要容忍缺字段。

### 6.7.1 归档里的旧 `report.json` 旁边**没有** `samples.csv`

v2.8.0 之前跑的 run,归档里只有 `report.json`;v2.8.0 之后才带 `perf_<dev>_<ts>.samples.csv` /
`.events.csv`。P5 的配对逻辑**只在报告存在时生效**,所以:
**如果脚本被硬停(来不及写报告),同一次的 CSV 不会被认领** —— 它们会留在 `reports/` 里,
而"报告没找到"的 note 会进 `summary.json`。这是刻意的(没有报告就没有锚点去配对),
但意味着**硬停的 run 其 CSV 需要人工从 `reports/` 捡**。

### 6.8 前端改动仍需你实跑确认

P1(定宽列)与 P6(事件行)的收益**完全是视觉的**,`py_compile` / `node --check` 测不出来。
且前端至今**从未在真实浏览器验证过**(见 §4)。用户 2026-09-14 已同意后续实跑确认。

### 6.9 perf_monitor v2 的已拍板决策与其残余风险(2026-09-14)

用户原话:「问我没用,我已经有点看不懂了……你拿一下主意,风险项可以暂时加到 todo 里面」。
以下由实现方按"保守 + 可反悔"拍板,**决定与理由见 [docs/PERF_MONITOR_V2.md](docs/PERF_MONITOR_V2.md) §12.7
与 [docs/DECISIONS.md](docs/DECISIONS.md)**。这里只记**残余风险**,不重复决策,也不重复 §6.2。

- **图表在超长跑里会退化成后段缩略图** —— `REPLAY_TAIL_BYTES=4MB`。v2.8.0 加了 `st` 列后
  单行约 309→332 B,所以帽从约 13981 行降到约 **12600 行**:T=2s 时 **≈ 7.0h**(原 7.8h)、
  T=1s 时 ≈ 3.5h(原 3.9h)。超过之后归档的 `chart.png` 只画得出后一段。
  **原始数据不丢**(`samples.csv` 是全分辨率的),但"一眼看全程"的那张图会缺头。
  推荐工作区间 **T=2s × 7h 以内**,再长就要先抬这个帽(或把 `st` 从实时行里去掉 ——
  它是唯一为"给人看"而增宽、却又不是给人看的列)。
  **v2.9.0 复核**:新增的 HTML 报告行只出现在**收尾那一次**的判定块里,不进实时流,
  所以这个帽**没有变化**(仍 ≈ 7.0h @ T=2s);逐拍线宽也一字未动。
- **samples.csv 被 Excel 独占打开时会降级** —— Windows 文件锁导致 append 失败,
  脚本落 `status=csv_degraded` 继续跑。此时**判读仍然成立**(evidence 来自内存,不读 CSV),
  丢的是**原始逐拍转储**。降级会印在判定块的 DATA 行并把结果封顶为 WARN。
- **设备侧 su 陷进 D 态会残留一个僵尸读进程** —— `timeout -k 2` 的 SIGKILL 对不可中断睡眠无效。
  PC 侧有界 `communicate` 保证**采集线程不死、任务照常跑完**,但设备上会留一个卡住的 `cat`。
  这是 POSIX 语义决定的,用户态无法解决;真要清只能重启设备。
- **WiFi 链路下超时余量未验证** —— 公式里的 adb 往返开销是 USB 实测(110–140 ms)。
  已改为**脚本启动时自测往返基线**(不再硬编码常数),所以 USB/WiFi 差异被自测吸收;
  但"自测值是否足够保守"本身没在 WiFi 下验证过。
- **`watch_pkg` 默认「不关注」** —— 不选时"应用崩溃回到 launcher"**不会被判成异常**(脚本不知道你关心哪个包)。
  默认行为与今天一致,是刻意选择;要抓这个场景就得选上。
  **v2.9.0 起它是下拉而非自由文本**,清单已由用户定稿(YouTube TV / Netflix / Prime Video /
  本地媒体播放器 + 不关注),改 `WATCH_PKG_CHOICES` 一处即可,前端零改动
  (`paramInputHtml` 早就支持 `select`,见 v2.2.0/v2.3.0 决策)。
  ⚠ **仍未验证**:本地媒体播放器(`com.mediatek.wwtv.mediaplayer`)的**进程名是否等于包名** ——
  `pidof <pkg>` 取不到 pid 的话 `fg_cpu` 对这个 app 恒为空。另三个已核对过(YouTube TV 3099 /
  Netflix 3314,都能取到)。跑它的时候顺手看一眼 `adb shell pidof com.mediatek.wwtv.mediaplayer`
  有没有输出。

### 6.10 gate 8(CPU)看不见「通道从来没被请求过」,而证据里没有能拆穿它的数据

**症状(v2.9.0 修掉的那个 bug 暴露出来的)**:前台通道整轮零数据时,gate 8 读的是
`fg_cpu["n_due_ok"]`,它当时是 **0**,于是门自己得出结论「没有预期中的采样」并**报 pass**。

**为什么 `n_due_ok == 0` 是歧义的**:它既可能是
(a) 运行太短,一次 SLOW 拍都没轮到(这时 pass 是对的),也可能是
(b) 代码把整条通道关掉了(这时该 FAIL)。
**证据里没有任何字段能区分这两者** —— `n_due`/`n_due_ok` 只在「到期」时累加,
而「到期」正是被写坏的那个东西。

**当前的兜底(不是修复)**:

- 假设备 harness 的 `assert_channels_alive()` 会在**测试**里抓住它(条件是「有 SLOW 拍成功返回过」),
  但那只覆盖 CI 路径,**真机跑不经过它**。
- 所以生产环境仍然只有**人肉看状态串**这一道 —— `samples.csv` 里 SLOW 拍恒为 `n` 而从不出现
  `b`/`f`/`h` 就是信号。判定块本身**不会**告警。

**要彻底关掉,需要往证据里加一个和「到期」无关的量**,比如:记录 SLOW 层的**理论开启次数**
(由总拍数与 T 直接算出,不依赖任何谓词),gate 8 在「理论应当开启过 N 次,而实际到期 0 次」时 FAIL。
这个量纯粹由 tick 计数推导,**bug 改不动它**,才算真正独立。**未做**(会动证据 schema 与判定版本号)。

> 同类形状的教训已写进 [docs/PITFALLS.md](docs/PITFALLS.md) #42:
> **形状断言看不见死掉的通道**。这一条是它在判定侧的残留。

**同族问题已修(2026-09-14,v2.9.0)**:gate 9(APP)原来也是"判据分不开两种状态" ——
把"关注的 app 从没出现过"判成丢失(假 fail),又看不见"窗口还在但进程没了"(假绿),
而后者正是该参数唯一的存在理由。现在拆成三出口(fail / inconclusive / pass),详见PITFALLS.md #44。
**本节的 gate 8 盲区仍然存在** —— 它和 gate 9 的病根相同(**判据可以被要检测的那个 bug 自己满足**),
只是 gate 9 能靠"先健康过没有"这个独立事实拆开,gate 8 目前没有等价物。

**v2.13.0 只关掉了其中一条歧义支路,不要误以为本节已解决**:「用户勾选时取消了前台应用」这条
现在是**显式可知**的(`run.monitors` 记着选择,门被 `MON_GATE_OF` 强制 `inconclusive`,见
[docs/PITFALLS.md](docs/PITFALLS.md) #52)。但 **(b) 那一条 —— 代码把通道悄悄关掉 —— 仍然
分不出来**,因为那种情况下 `run.monitors` 会显示这一路**是开着的**。本节要的"与到期无关的
理论开启次数"仍然没做。

### 6.11 `capped_by` 算出来了,但全仓没有任何渲染层读它(既有空洞,v2.12.0 时发现)

**症状**:`judge()` 会返回 `capped_by`(形如 `['-2=warn(UNCALIBRATED)', '-3=warn(WIFI)']`),
但 `format_result_lines` / `verdict_rows` / `format_result_html` **三处都没消费它**;而 `GATE_ZH`
只在 HTML 的门表里被用到,伪门又**不在 `gates` 里**。⇒ 伪门 `-1 PARTIAL` / `-2 UNCALIBRATED` /
`-3 WIFI` **从来没在报告上露过面**。

**为什么不是 v2.12.0 顺手修的**:v2.12.0 的 WiFi 封顶**改成显式渲染两处**(`WIFI` 行的值自带摘要 +
HTML `六、` 小节写明封顶),所以结论对用户**是可读的** —— 补 `capped_by` 是**独立**的一件事,
不该和"加一个监控项"绑在一起做。

**要做什么**:把 `capped_by` 接进 `verdict_rows()` 或 HTML 的一条 `sub` 行,让**任意**伪门的原因
自动可见,而不是每加一个伪门就手工补一次渲染。**注意别踩两条既有约束**:① 判定块里除最后一行外
**不得出现字面量 `report`**(PITFALLS.md #43);② HTML 里不得出现裸 `None`。

**风险**:低(纯渲染层,不动判定),但它会改动**每一份**报告的判定块,属于需要单独眼验的改动。

### 6.12 `NetworkCapabilities` 的能力位没验证,所以没写那个解析器

`cmd wifi status` 的输出里除了 `WifiInfo`,后面还有一整块
`NetworkCapabilities: [ Transports: WIFI Capabilities: NOT_METERED&INTERNET&...&VALIDATED... ]`。
**这是比 ping 更好的 L3 信号** —— 它是 Android 自己对"这条网到底能不能上网"的权威答案
(`INTERNET` / `VALIDATED`),零额外开销(同一份输出里)。

**但没在这台设备上读出真实的 token 列表**,所以 v2.12.0 **不写那个解析器** ——
`--probe` 会把整块原样打印出来供人读。**读到真 token 再谈**,不要照文档猜。

**注意**:`TransportInfo:` 里的 `SSID:` 是占位符 `<unknown ssid>`,且 `IP:` / `RSSI:` 与
`WifiInfo:` 那一份**会不一致** —— 见 [docs/PITFALLS.md](docs/PITFALLS.md) #49。

### 6.13 每段 guard 之和可能超过 PC 侧预算 `B`(既有几何问题)

设备侧每段各有 `timeout -k <k> <G>`(GPU 最多 5 s),而 PC 侧整个 adb 调用的预算 `B ≈ 5.5 s`。
v2.12.0 又加了一段 `timeout -k 1 2`(WIFI 最多 3 s),**让这个几何问题更容易发生**。

**失败模式是响亮的,不是静默的**:`res=timeout`、该拍所有 metric 判 `ST_FAILED`、判定块里
`EVENTS : timeout=<n>` 直接可见 —— 所以**不是**"看起来一切正常"那类危险。

**要做什么**:要么把 `B` 从"各段 guard 之和 + 余量"推导,要么给各段一个总预算并分配。
**不在 v2.12.0 改 guard 模型。**

### 6.14 WiFi 读在**负载下**的耗时没测

`--probe-cost` 报的 98–172 ms / +76.5 ms 都是**空载**。这台设备的存在意义就是在 CPU 负载下放视频,
那时 `system_server` 与 wifi 栈都会更忙。**若翻倍,T=2s 的占空比会从 9.43% 走到 ~13% 上下。**

`--probe-cost` 结构上只能量空载;**要测得开着一个视频跑**。另外 `adb over WiFi` 链路
(§6.3 的既有条目)**完全没测** —— WiFi 通道自己受影响会更大。

**同一次重测还推翻了一个已发布的数字**:2026-09-14 记的 `FULL 408.7 ms / 占空比 9.07%`
在 2026-09-20 复测时变成 `FULL 383.9 ms`,即**那台设备当时慢了近一倍**。⇒
**占空比的绝对值不是常数,只有同机同批的增量可比(+76.5 ms)**。详见
[docs/PERF_MONITOR_V2.md](docs/PERF_MONITOR_V2.md) §12.1.1。

### 6.15 NTC 的两处已知缺口(v2.13.0;第 1 条于 v2.13.1 修正)

1. ~~**两个通道共用一个 `ntc_st`,半失败无法归因**~~ —— **这条原来的说法是过头的,已修正**。
   两个通道确实共用一个状态字符(新鲜度是**命令级**属性,它们来自同一条命令的一次读取),
   那个字符本身也确实分不出是哪个节点读失败了;**但归因并没有丢**:两列是分开的,
   **哪一格是空的就说明是哪个节点**,`n=0` 也只出现在那一个通道上。采集侧真正的代价只是
   "状态字符不能代替去看那一列"。派生文件里另有 `ntc_lcd_st` / `ntc_led_st` 两列逐通道状态
   (v2.13.1 的 [tools/ntc_convert.py](tools/ntc_convert.py))。
2. **NTC 读在负载下的耗时** —— 空载 +51.5 ~ +86.4 ms/拍(它单独一次 `cat` 只 +31.4 ms)。
   v2.13.1 用**合成负载**测过一次(4 核 6 个 `timeout 120 yes`,~98% 忙):**NTC 自己的 delta
   几乎不动**(空载 +73.0 → 负载 +66.4 ms),而同一轮里 WiFi 的 delta 从 +68.6 涨到 +213.3 ms。
   **仍然没测的是"设备真在放视频"那种负载** —— 与 §6.14 属同一类未知。

### 6.16 ~~取消「前台应用」后,`APP` 事实行仍打印 `STABLE switches=0 pkgs=0`~~ —— **已修,v2.13.1**

原状:取消勾选前台应用后,`APP` 那一行仍打出 `STABLE switches=0 pkgs=0`,读起来像"看过并且
没发现问题",而真相是这一路本次根本没采过。**v2.13.1 已修**:`fg`(以及 `mem`)被取消时,
该行输出 `NOT MONITORED (... the 'fg' monitor was switched off for this run ...)`,并且
**不再打印那串全零计数器** —— 打印 `switches=0 pkgs=0` 正是这一行要防的误读。两个条件行
(`WIFI` / `NTC`)同样把"本次被关掉"和"读了但什么都没读到"分开说。**事实行始终有值**这条
断言(`verdict_rows` 的既有断言)没有被破坏,另有 5 条新断言钉住这三种情形。详见
[docs/PITFALLS.md](docs/PITFALLS.md) 的"条件行"纪律与 `scripts/perf_monitor.py` 的 `_display`。

### 6.17 `--probe-cost` 的变体是"逐个量完再换下一个",背景漂移混进了差值(v2.13.1)——**已修**

> **结案(2026-09-20,`perf_monitor.py` 1.3.2)。** `run_probe_cost` 已改成 **round-robin**:
> 外层是轮次、内层是变体,中位与打印挪到后面独立的一遍(保证表格仍按代价顺序读)。
> **adb 调用次数 / sleep 次数 / runtime 全部不变。** 新增 3 条断言,其中两条在旧的块采样上
> **必须变红** —— 已用一份"只把循环顺序改回块状"的副本反身验证过(2 failure)。
>
> **但"改完更准了"这个结论,实测没量出来,所以不许写。** 最初拿 round-robin 三次(极差 6.8)去比
> 文档里块采样三次(极差 34.9),看着好了 5 倍 —— **那是错的比较**,两组是不同时刻的背景。
> 补做受控 A/B(同一天两种取样**交替**各 3 次):round-robin `+51.8 / +58.6 / +68.7`(极差 **16.9**),
> 块采样副本 `+63.3 / +64.5 / +71.8`(极差 **8.5**)—— **块采样那组反而更紧**。
> n=3 什么都说明不了。全部可用证据:round-robin 7 次落在 **+51.8 ~ +74.0**,
> 块采样 6 次落在 **+51.5 ~ +86.4**,散度同量级。
>
> **负载下又做了一遍(交替各 n=6),同样没有优势**:ntc delta 极差 247.6 vs 225.7,wifi delta
> 极差 223.3 vs 255.3,**两种顺序各出现一次负的 wifi delta**(−7.2 / −23.0)—— 行间散布已经大于
> 通道本身的代价。**负载下真正的主导误差是"每一行自己就没测准"(组内极差 200~300 ms),与取样
> 顺序无关** —— 见本文件 §6.20。
>
> **改动仍保留**:它去掉的是**系统性**误差(块采样把两行之间那段漂移整段记进差值,而差值正是这张表
> 唯一的产品),代价为零。**"这次没量出来" ≠ "这项误差不存在"** —— 但反过来也一样,
> **两次受控实验(空载 3+3、负载 6+6)都没量出改善,而且两次都是块采样极差更小;
> "实测证明它更稳"是假的。**
> [docs/PITFALLS.md](docs/PITFALLS.md) #53 的规矩②现在由代码保证,不必再靠人手工交错;
> [docs/PERF_MONITOR_V2.md](docs/PERF_MONITOR_V2.md) §12.1.1 的锁表已换成 round-robin 实测值,
> 并如实记下"没量到改善"。

以下为结案时保留的原始记录。

`run_probe_cost` 对每个变体**连续量完 reps** 再换下一个变体,于是每个变体采到的是**不同时刻**
的设备背景状态,相邻变体之间的差值里混着背景漂移 —— 这正是"占空比不是常数"的机制,
已作为规矩记进 [docs/PITFALLS.md](docs/PITFALLS.md) #53。

**建议的改法(本次只做解释,未改代码)**:变体**交错取样**(round-robin:一轮里每个变体各一次,
再进下一轮),让背景漂移在**差值**里对消。**调用次数不变**(现在也是"每个变体 reps 次"),
**runtime 不变、sleep 次数不变**。

> 更正:先前这里还写了"输出里同时给出离散度(min/median/max)"当作待做项 —— **它已经在印了**
> (`run_probe_cost` 的表头就是 `median ms / min ms / max ms`)。而且**正因为它在印,才看得出当前
> 设计的盲区**:min/max 量的是**同一变体在一个连续块内**的离散度,而污染恰恰发生在**块与块之间**
> —— 块内漂移是常数、看不出来,块间漂移又和信号混在一起。已打印的离散度**不足以**发现这个问题。

### 6.18 NTC 的 B 值:配置(4010)与厂商参考实现(>3588 Ω 时切 3950)不一致(**2026-09-21 已结案 → D-64**)

> **结案(2026-09-21)**:厂商的实际实现拿到了 —— `transformNTCToTemp` 是一段**通篇字面量、不读任何
> 配置**的 Java。所以"实现 vs 配置"这个二选一里,**实现才是板子上真正跑的那个**,用户裁决
> 「按 Java 实际函数做最终的,配置可以留作 temp_template,后续我找开发确认下」。改成**两段 B**:
> `NTC B VALUE COLD` / `COLD2` = 3950 与 `NTC B SWITCH R` = 3588 Ω(**严格大于**);三键要么都给、
> 要么都不给,只给一半**报错**。
>
> 顺带查出一件事:工具原来的 `REFERENCES` 真值回归表 **7 个锚点全部**等于单 B 列,**没有一个**对得上
> 厂商的两段规则 —— 那句 "valued with the vendor's reference implementation" 是**说反了**,旧表测的是
> 工具自己,不是固件。新表两列都留,断言打在**两段**列上;把分段那两行注释掉,新断言**红 9 条**。
>
> 实际差别:LCD 节点 **+0.05 °C**、LED 节点 **0.000 °C**、冷端 adc=800 **−0.163 °C**。归档的
> `samples.csv` 不受影响。记录在 [D-64](docs/DECISIONS.md)、[CHANGELOG](CHANGELOG.md) 的
> `tools 1.2.0` 节、[tools/README.md](tools/README.md)。

以下为结案时保留的原始记录。

厂商的参考实现在算出的分压电阻 **> 3588 Ω** 时会**换成 `B = 3950`**,而项目的 NTC 配置里只有
单一 `B = 4010`。2026-09-20 用户裁决**暂时以配置为准**(原话:「现在暂时是按配置里面的」)。

**这块板子现在读数落在两者差 < 0.05 °C 的区间内,所以不影响当前结论**;但这**不是等价** ——
一旦某天读到更靠曲线一端的值,两种取法就会给出不同的温度,届时必须重议。工具的 `# params=`
表头行**会把实际用的 `b=` 印出来**,所以不会有人靠猜。记录在
[docs/DECISIONS.md](docs/DECISIONS.md) D-63 与
[tools/ntc_profiles/9660_P53_2G.ini](tools/ntc_profiles/9660_P53_2G.ini) 的注释里。

### 6.19 换算/画图**不自动触发**,平台侧的交互方式待定(2026-09-20)——**2026-09-21 结案**

现在跑完一次压测,归档里**只有原始 ADC**;要摄氏度和曲线图,得自己记得跑一条
`python tools/ntc_convert.py <那个 samples.csv>`。2026-09-20 用户实测走了一遍这个路径,直接问
「换算后的在哪里」—— 摩擦是真的。

**用户裁决:先不改成自动调用**(原话:「先不改成自动调用吧,后续我想一下交互方式」)。所以这条
**不是待办而是待议**:可能的形态有①平台在 perf 跑完后自动调一次(默认 profile);②归档目录里放
一个说明/一个双击就跑的入口;③前端出一个"换算"按钮。**没有选择之前不要动手**。

注意①③里那个「默认 profile」是个真问题:换项目时忘了换 profile,就会悄悄出一份**用错参数**的
摄氏度。工具的 `# params=` 表头行印了实际用的常数,但那是事后核对,不是事前防错。

**2026-09-21 结案:用户选了①,并把它做成前端可配参数。** 上面那条担忧是**正面回答**掉的,不是绕过:
- `perf_monitor` 的 `finish()` 末尾自动调一次换算,归档目录里多出 `<stem>.samples.temps.csv` 与
  `<stem>.samples.temps.png`。图能进 `archive/` 靠的是归档白名单放宽到 `.png`(见决定 D-67);
- 那个「默认 profile」按原担忧处理:profile 变成前端参数 `ntc_profile`(默认 `9660_P53_2G`),**且工具的
  默认档规则收紧为「只有恰好一份 profile 时才敢猜,两份及以上必须显式指定」** —— 事前防错,而不是
  事后核对表头;
- profile 文件也更名为 `tools/ntc_profiles/9660_P53_2G.ini`(项目名),并删掉本工具根本不读的键。
- 裁决记录:[docs/DECISIONS.md](docs/DECISIONS.md) D-65(自动换算)、D-66(profile 参数化 + 默认档收紧)、
  D-67(归档白名单)。

### 6.21 **硬停(SIGKILL)那条路径不触发 NTC 换算**,归档里会缺温度和图(2026-09-21)

D-65 把自动换算挂在 `Monitor.finish()` 末尾。**「硬停」按钮直接 SIGKILL 子进程,`finish()` 根本没跑** ——
所以那条路径的归档里只有 `samples.csv`(原始 ADC),**没有 `*.temps.csv`、没有 `*.png`**。

数据没丢,可以手工补:

```
python tools/ntc_convert.py archive/<模块>/<时间>_<脚本>_<设备>/<那个>.samples.csv --profile 9660_P53_2G
```

产物会落在同目录(契约是"输入旁边"),补完即与正常跑完的归档一致。

**没有做成自动兜底,是决定**:在 `server.py` 的归档流程里再加一处"没有 temps 就补算"会形成**第二个触发点**,
而两个触发点各持一半规则正是 [docs/PITFALLS.md](docs/PITFALLS.md) #51 记过的形状。等真机跑出过这种归档、
或者用户明确要求,再重议。

### 6.22 ✅ **已结案(v2.14.1,2026-09-21)** —— NTC 两个 sysfs 在 SELinux Enforcing 下对 `shell` 域是拒绝的

> **结案说明**:用户当日报了这个症状(「我能 `cat` 出来,但脚本显示 NTC off,没任何打印」),裁决 **「直接写死 `su 0`」**,即下面的方向 1;方向 3 一并做了(探针在 stdout 为空时回显 stderr)。裁决与禁止项见 [docs/DECISIONS.md](docs/DECISIONS.md) **D-68**,机理与判据见 [docs/PITFALLS.md](docs/PITFALLS.md) **#55**。**以下原文保留,不重写。**
>
> 结案后仍然成立的两条:① **设备不能 root 就丢 NTC**(降级关闭,与 GPU 同一处理);② 代价 **+60.6 ms/拍**。
> 新增一条待办事项见 §6.23。

本次做 v2.14.0 真机验证时撞上的,与本次改动**无关**,但会让 NTC 静默变成"本次没采到":

```
$ adb shell "cat /sys/bus/iio/devices/iio:device0/in_voltage3_raw"
cat: .../in_voltage3_raw: Permission denied          # uid=2000(shell)
$ adb shell id
uid=2000(shell) ... context=u:r:shell:s0
$ adb shell getenforce
Enforcing
$ adb shell su 0 cat .../in_voltage3_raw            # 64492
678                                                  # OK
```

**文件模式确实是 `-rw-r--r-- root root`**(所以 README [README.md](README.md) 第 416 行、449–450 行写的"**免 root**,world-readable"是**照文件模式写的**),但 `ls -lZ` 显示它的 SELinux 标签是 `u:object_r:sysfs:s0`,**Enforcing 下 `shell` 域读不到** —— 模式对了,策略不对。

**现场证据表明这是环境变了,不是一直如此**:2026-09-20 19:03 那次归档的 `ntc_st` 是 `f:1, h:11`(真的读到了);2026-09-21 上午同一条命令变成 `Permission denied`,`ntc_st` 全是 `d`(整段禁用)。中间设备上发生过什么(重启?固件?`adb unroot`?)**没查**。

**行为是可见的,不是静默**:控制台打 `[probe] ntc parse : FAIL - ntc channel will be disabled`,报告 `NTC : TEMP ? no ntc observation in this run`,SCOPE 行会说明。所以不会有人拿一份"没有温度"的归档当"温度正常"。

**要改的话有三个方向,本次一个都没做**(改动会碰到采集侧契约,得先对齐):
1. 把 NTC 那两段 `cat` 改成 `su 0 cat`(GPU 那条路已经是 `su 0` 的先例)—— 代价是**设备没 root 时连原来能读的机器也一起读不到**;
2. 保持现状,把它写进 README 的"运行前提":**跑 NTC 前先 `adb root`**;
3. 先补一个"探不到就说清楚"的诊断(现在 `[probe]` 那行只印 FAIL,不印 `Permission denied` 这个**原因**)。

**当前设备状态:我已经跑过 `adb root`**(adbd 现在是 root,`adb unroot` 可还原)—— 不 root 就验证不了本次改动。**下次设备重启后 adbd 会回到 `shell`,NTC 会再次读不到。**

### 6.23 NTC 换 `su 0` 之后,**复合命令里那一档的增量没有重量**(2026-09-21)

v2.14.1 只测了**单次**读:`su 0 cat` 中位数 **200.1 ms** vs 裸 `cat` **139.5 ms**(+60.6 ms)。

但文档里那几个被引用得最多的数是**段并入复合命令之后**的增量:`README.md` 的 **+51.5 ~ +86.4 ms/拍**、
[docs/PERF_MONITOR_V2.md](docs/PERF_MONITOR_V2.md) §12.1 的 "+65 ms 上下"。这两个数**没有随本次改动重测**,
现在应当整体上调(粗估 +30 ms 量级,因为 `su` 与 shell 启动的开销会重叠,不会直接加上 60.6)。

**为什么单独立一条而不是顺手改数**:[docs/PITFALLS.md](docs/PITFALLS.md) #53 记的就是"量到的不是设备的常数,
是这台设备此刻在忙别的" —— 增量只能在**同一次测量内的相邻两行**之间比。拿今天单测的 60.6 ms 去**推算**
复合命令里的增量,正是那条规矩禁止的做法。

**要做**:跑一次 `--probe-cost`(或短跑一拍一次的手工测量),在**同一次测量**里同时拿到裸 `cat` 段与
`su 0 cat` 段的交错读数,然后把 README / PERF_MONITOR_V2 里的那两处数字一起换掉。**不做**:直接 +60.6。

### 6.20 `--probe-cost` 在负载下根本没测准,而"加 reps"这条路**没做实验**(2026-09-20)

负载下(6 个 `timeout 400 yes`,~66% idle)`--probe-cost` 的**逐通道 delta 不能用**:两种取样顺序
各 6 次里,ntc delta 极差 247.6 / 225.7 ms,wifi delta 极差 223.3 / 255.3 ms,而且**两种顺序各出现
了一次负的 wifi delta**(−7.2 / −23.0)—— 一个纯成本差值出现负号。

**已经查明了一半的原因**:同一次测量里,**同一变体自己**的 5 次取样就散得厉害 ——
`FULL` min 627.0 / max 924.6(组内极差 **297.6 ms**),`FULL+WIFI` 298.2,`FULL+WIFI+NTC` 198.0。
**组内极差(200~300)与组间极差(220~255)同量级**,所以"两行是否同时刻"不是瓶颈,**每一行自己
就没测准**。改取样顺序治不了这个(已试过,见 [docs/PITFALLS.md](docs/PITFALLS.md) #53)。

**待做**:① 加 reps 能不能压下来?压到可用需要多少 reps?(`--probe-reps` 已经存在)② 或者把
`min` 当估计量而不是 `median` —— 负载下最快的那次最接近"没有排队"的真实代价,这一条**只是猜想,
没有理论依据也没有数据**。③ 有没有必要在负载下量逐通道代价?平台实际关心的是**总额**(跑一次的
占空比),而总额在负载下的信号(FULL 428.7 → 877.5 ms)远大于噪声 —— 也许结论就是"负载下别问
逐通道"。**没有先想清楚③,不要动手做①②。**

---

### 6.24 归档里那张**坏图还是坏的**(v2.14.2 只修了工具,没碰历史产物)

`archive/perf/20260921-180514_perf_monitor_435E54A1BB0D08004C10/` 里的
`perf_435E54A1BB0D08004C10_20260921_180514.samples.temps.png`(52995 字节)是**修复前**渲染的,
所以它**仍然是那张没有曲线的图**。同目录的 `.samples.temps.csv` 正常,`samples.csv` 更是一个字节没动。
(那个目录里被顶到 08:47 的时间戳是**归档动作**留下的,不是文件内容变了。)

**要重出是能做的** —— 工具的 `--force` 就是"输出已存在时覆盖",输入是归档自己那份 `samples.csv`:

    python tools/ntc_convert.py "archive/perf/20260921-180514_perf_monitor_435E54A1BB0D08004C10/perf_435E54A1BB0D08004C10_20260921_180514.samples.csv" --force

**但已裁决:不主动改。** 两条理由:

1. `archive/` 在本项目里是**永久保留**的产物,改它等于改历史 —— 而"归档里那份 == 当时跑出来的那份"
   本身就是一条有用的性质(它记录了 bug 确实存在过,以及它长什么样);
2. 工具修好之后再跑一次长跑会产出**新的一份**,两份并排比"覆盖掉一份"信息更多。

用户点名就重出。**在那之前谁都不许动这个文件。**

## 7. v2.10.0 报告引擎的已知残余(2026-09-16)

> 都是**已确认、当前决定不做**的事。设计决策与实测结论在
> [docs/DECISIONS.md](docs/DECISIONS.md) 的 v2.10.0 条;格式真源在
> [docs/REPORT_FORMAT.md](docs/REPORT_FORMAT.md)(该文 §12 有同一份清单的简述)。
> **7.1(中断不写报告,当天即解决)与 7.6(引擎行数估算)已移除** —— 号保留,不复用。

### 7.2 `wifi_onoff` / `sensor_reboot` / `battery_inout` 的报告没在真机上跑过

这三个脚本只过了静态门(`py_compile` + `--dump-params` 字节一致),**没在真机上执行过**。
`wifi_switch` / `app_launch` / `perf_monitor` / `bt_reboot` / `wifi_reboot` 五个都真机跑过。
下次连上设备时优先补这三个。

### 7.3 引擎的 `attach()` / `document_of()` 是死 API

定义了、有 docstring,但**全库无调用方**(`document_of` 的 docstring 原本谎称"被 `--selftest` 用于验证往返",
本轮已改成如实说明它是预留)。保留理由:它表达的能力(只拿 JSON 也能重建页面)是合理的未来需求,
代价只有约 10 行。**觉得碍眼就删掉这两个函数**,没有调用方,删了不会破任何东西。

### 7.4 `app_launch_stress` 一次运行多份报告,前端按钮只开第一份

它每个 APP 写一份 json + html,所以一次运行可能有 3 份。`GET /api/tasks/{id}/report` 按文件名排序
**只返回第一份**(按钮的 tooltip 里写了共几份,其余从「存档」进去取)。**要不要为它加多选 UI 未定。**

### 7.5 明文 Wi-Fi 口令仍在归档的 `report.json` 里

`doc["hide_keys"] = ["password"]` **只管 HTML 渲染**。`archive/wifi/.../report.json` 仍含
`WIFI_NETWORKS` 的三条明文口令。这是既有行为(平台从不解析报告 JSON,也不该开始解析),
但**分享那份 JSON 前必须自己删**。HTML 已验证不含任何口令。

> 用户长期指令:**Wi-Fi 口令不算敏感,不要再提**。本节只作事实归档。

### 7.7 `reports/` 里留着本轮验证的产物(**已定:暂不删**)

2026-09-16 的验证跑在 `reports/stress-test/{wifi,app-launch,perf}/` 留下 6 组 json+html,
另外 `reports/_demo_report.html` 是做引擎冒烟测试时的手工件。**它们全部被 `.gitignore` 覆盖**
(整个 `reports/` 目录都不入库),所以不进版本库、也不会被任何将来的任务认领
(mtime 兜底扫描要求 mtime ≥ 任务开始时间)。**用户 2026-09-16 已定:暂不删** —— 留作引擎的手写样例与这轮的验证痕迹;哪天真要清就整目录删掉。

### 7.8 `app_launch_stress` 的热启动(hot)模式还没真机验证

**冷启动已真机验证通过**(三个 APP 各 1 轮,`TotalTime` 正常、`LaunchState=COLD`、
logcat `Displayed` 交叉验证 1/1 命中)。**热启动模式没跑过** —— 下次跑一轮确认
HOME 退后台 → 拉起流程与 WARM/HOT 语义校验是否符合预期。

---

## 8. v2.11.0 按键 keepalive 的已知残余(2026-09-17)

> **8.1(硬停后设备永久卡死)已修于 v2.11.1** —— 号保留,不复用;逐字记录见
> [CHANGELOG.md](CHANGELOG.md) v2.11.1 与 [docs/DECISIONS.md](docs/DECISIONS.md) 的「单一终结者」条。

### 8.2 往 `ir_sequences/` **新加** ini 后,下拉框要等重启服务才出现

平台按 `(脚本名, 文件 mtime)` 缓存 `--dump-params`,
而 `KEY_INI_CHOICES` 是**导入时**扫目录生成的 → **改已有 ini 的内容随时生效**(节奏立刻变),
**新增一个 ini 文件**则要等服务重启或 `perf_monitor.py` 本身变动。**不为它加缓存失效机制**(收益极小)。

> **v2.15.0 起这条影响两个脚本**:`perf_monitor.py` 的 `key_ini` **和** `power_cycle_stress.py` 的
> `ir_sequence` —— 两者都在**导入期**扫 `ir_sequences/*.ini`(`_key_ini_choices` / `_ir_ini_choices`,
> 后者是前者的逐字对照)。所以给硬开关机压测新写一条开机后动作序列时,**同样要重启服务**它才会出现在
> 下拉里。判定不变:收益极小,不修。

### 8.3 keepalive 的 adb 调用会与每拍采样争用同一台设备

一次按键 ≈ 一次 `adb shell input keyevent`,与当拍的复合采样命令抢同一个 adb server。
`cost_ms` 变大**只会抬高**预算 —— PC 侧 `B` 由最近 30 个 tick 中 `res ∈ {ok, partial}` 的 `cost_ms`
估计窗派生(必须排除 timeout),设备侧 `G` 随之放宽(`timeout -k 2 G <cmd>`)—— **不会误杀**;
代价是报告里的 `COST`/`duty` 读数会**略微上浮**。判定模型见 [docs/PERF_MONITOR_V2.md](docs/PERF_MONITOR_V2.md) §12.3。30 分钟一次的节奏下影响可忽略,但归因时要知道参数表里选了哪个 ini(报告 `config` 里有 `key_ini`)。

### 8.4 中断恰好落在 `Long` 长按的「按下」与「抬起」之间 → 设备可能停在按下态

`run_step` 的 Long 分支里没有 `finally` 抬起。用 `Short` 步(本仓库附的模板就是)不涉及。
**不在 keepalive 里补 `finally`** —— 那要改 `ir_runner.run_step`,而它是三个脚本共用的执行器。

### 8.5 两个线程同时 `print` 时,极低概率吃掉前端一个采样点

CPython 的 `print` 是**两次 write**(先写内容、再写 `\n`),所以两个线程的 print 可以**在同一行交错**
(本轮实测到一次:开跑瞬间 `[key]` 横幅与第一次按键的 `  -> KEYCODE_MEDIA_PLAY` 粘成一行 ——
已通过「横幅在起线程之前打印」消掉那一处)。运行中剩余的风险:按键行与主线程的 `PERF|{...}` 采样行互撞,
会让 `static/app.js` 解析失败 ⇒ **图表少一个点**。窗口是「主线程两次 write 之间」的微秒级,
每秒只有 1 次采样机会,而按键 30 分钟才 1 次 ⇒ 8 小时长跑里的概率约 `1e-5` 量级。
**不为它加进程级 stdout 锁**(要改几十处 print,属于过度设计);如实记在这里。

### 8.6 `key_sent` 的口径:一次 `run_step` 调用 = 一次按键

所以 `count=0` 的步不计数(与 `run_step` 行为一致),而**被打断的那一次按键不计**
(它可能已经按下去了,但 `run_step` 没返回)。**这是保守方向**:宁可少报,不虚报。

---

## 9. 平台的会话与清理残留(2026-09-18 归档)

两条都是**已知、当前不做**的小问题,不影响数据正确性,但知道形状能省掉一次排查。

### 9.1 服务重启后,页面的日志面板「卡」在最后一行

WS 在 server restart 时会断,而前端**没有自动 rejoin**(log WS 只有手动重连路径)。
表现:日志 console 停在上一次的最后一行不再增长,但任务其实还在跑。
**绕法**:重新点一次任务(`viewTaskLogs`)或刷新页面。**不做自动重连** ——
`ir_runner` 任务之外都是纯客户端流,加自动重连要在 `openWs`/`scheduleReconnect` 上再叠一层陈旧防护,
收益不抵风险。

### 9.2 任务被删除后,它的 log 文件没有任何自动回收

**实测**(2026-09-18 逐处读 `server.py`):删任务只走 `_forget_task()`,其 docstring 逐字写着
**"Deliberately touches NOTHING on disk"**、处置说明 "left for the user to clear by hand"。
全库对 `LOGS_DIR` 只有**写**(L265 / L1361 / L1989)与**建目录**(L73),无任何 `unlink` / `glob`;
仅有的删除是 `ir_sequences/_seq_*.ini`(startup,L408–410)、`_archive_task` 搬走后的 `reports/`
原件(L1392 / L1421)、以及用户点名删的 `.ini`(L1908)。
⚠ `_reap_orphan_scripts()` / `_reap_orphan_logcat()` 清的是**崩溃残留的进程**(前者 `python.exe`、
后者 `adb logcat` 子进程),**不是文件** —— 别再说"startup 会把 log 文件清掉"。
**刻意不做自动回收**:`archive/` 是耐用副本,`logs/` 只是运行期工作区。

### 9.3 删除任务的确认框在说谎:它说日志会被一起删掉

**严重度:低(不改数据,只误导用户)** —— 但方向很坏:它让用户以为删任务会销毁产物。

`static/app.js` 里两处确认文案都写着「日志文件会一起删除,无法恢复」(删单个任务、批量清空),
而**同一个界面**上任务卡 `×` 按钮的 tooltip 写的是「从列表移除(存档保留在 archive/)」。
**后者才是事实**:`_forget_task()` 刻意不碰磁盘,归档永久保留;`logs/` 里的运行期文件确实不会被删,
但那是"残留待清"(§9.2),不是"被删掉"。

**改法**(未做):两处确认文案改成与 `×` tooltip 一致的措辞 ——
「从列表移除;归档保留在 `archive/`,`logs/` 里的临时文件需要手工清理」。

---

## 10. v2.15.0 硬开关机压测的已知残余(2026-09-23)

> 本节的每一条都是**已知、有意不修**。其中 10.1 是这一组里唯一一条**会真的丢产物**的。

### 10.1 硬停(SIGKILL)不跑 `finally` ⇒ 那一次运行**没有报告**

`power_cycle_stress.py` 的报告写在收尾的 `finally` 里(所以"中断"能拿到报告)。但平台「硬停」走的是
**SIGKILL** —— 进程没有机会跑任何收尾代码,于是 `reports/` 与 `archive/` 里**都不会出现这份报告**,
只有 `logs/` 那边的 stdout/logcat 原样留档(它们由**平台**采集,不依赖脚本)。
**与 §6.21 是同一个形状**(那次丢的是 NTC 换算)。

**症状长这样**:归档目录里没有 `report.json` / `.html`,但 `stdout.log` 完整。
**处置**:手工重跑,或直接读 `stdout.log`(逐轮结果都打在控制台上)。
**不修的理由**:要在服务端加兜底就得再造一个"谁来写报告"的触发点 —— 正是 PITFALLS #51 记过的那个形状
(同一个开关量被两个调用点各拿一半)。收益(一次异常路径的产物)不抵风险(两处发报告的口径漂移)。

### 10.2 整个嗅探循环**从未在真机上跑过**

截至 v2.15.0,验证过的只有:`--selftest`(27 条纯函数断言)+ 一套**模拟继电器台架**(monkeypatch 掉
adb 探针,驱动真实 `main()` 跑完 4 个场景,39 条断言,退出码 0,并核对报告 JSON + HTML 落盘与 stdout 的
`html =` / `report :` 顺序)。

**没验过的一律没验过**:真实继电器的通断节奏、`adb devices` 的重新枚举时序、真实的 `sys.boot_completed`
时间线、`run_step` 的**真机**按键派发。**无硬件即无证据**,别把上面那两条说成"验过了"。
**建议**:先 `iterations=2` 短跑一次确认整条链路,再上默认的 1000 轮。

### 10.3 `run_step` 的 `Long` 分支没有 `finally` 抬起 —— 本脚本也会踩

与 §8.4 同源,但**本脚本的窗口更大**:`perf_monitor` 的按键 30 分钟才 1 次,而这里是**每轮一次**
(一轮 ≈ 10 分钟)。中断/异常恰好落在 `Long` 长按的「按下」与「抬起」之间 ⇒ 设备可能停在按下态。
**不修**:要修得改 `ir_runner.run_step`,而它是共享执行器(D-29 / §8.4 同一裁决)。
**规避**:开机后动作序列里**只用 `Short` 步**(本仓库附的模板都是 Short)。

### 10.4 报告里的"按键"列**不保证 YouTube 真的播起来了**

它记的是"**发了多少次键 + 派发是否失败**"。序列文件是一串键码,脚本无从知道设备那边发生了什么
(YouTube 防自动化、首屏不是推荐页、播放失败 —— 全都不在 IR 的反馈面上)。
**这一列看起来像在保证播放,其实不是**,报告里已写明;不要在别处把它当成功判据引用(D-71)。
