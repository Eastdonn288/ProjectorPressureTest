"""IR (infrared remote) tools package.

Contents:
    ir_remote.py   - IRRemote class for ADB sendevent operations
    keyevent.txt   - default keycode mapping (parsed by IRRemote)

Used by:
    scripts/ir_runner.py  - PPTP platform script

These modules are library code, not platform scripts. They are not
auto-loaded by the platform's /api/scripts endpoint.
"""